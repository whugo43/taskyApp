import { FiltroAdopcionPipe } from './filtro-adopcion.pipe';

describe('FiltroAdopcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAdopcionPipe();
    expect(pipe).toBeTruthy();
  });
});
