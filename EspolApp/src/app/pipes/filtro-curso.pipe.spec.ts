import { FiltroCursoPipe } from './filtro-curso.pipe';

describe('FiltroCursoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCursoPipe();
    expect(pipe).toBeTruthy();
  });
});
