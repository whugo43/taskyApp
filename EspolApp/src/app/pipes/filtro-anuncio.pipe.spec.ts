import { FiltroAnuncioPipe } from './filtro-anuncio.pipe';

describe('FiltroAnuncioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAnuncioPipe();
    expect(pipe).toBeTruthy();
  });
});
