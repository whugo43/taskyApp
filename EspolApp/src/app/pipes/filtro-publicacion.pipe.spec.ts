import { FiltroPublicacionPipe } from './filtro-publicacion.pipe';

describe('FiltroPublicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPublicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
