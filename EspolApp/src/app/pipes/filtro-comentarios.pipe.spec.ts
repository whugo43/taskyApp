import { FiltroComentariosPipe } from './filtro-comentarios.pipe';

describe('FiltroComentariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroComentariosPipe();
    expect(pipe).toBeTruthy();
  });
});
